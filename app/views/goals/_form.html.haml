= form_for goal do |f|
  .form-group
    .form-row
      .col-md-10
        I pledge to travel
        = f.text_field :total, placeholder: current_user.imperial_system? ? '100,000' : '30,000',
          value: @goal.converted_total
        human-powered
        = f.select(:metric, Goal.metrics_select_values(current_user))
        between
        = f.date_field :start_date
        and
        = f.date_field :end_date
      .col-md-2
        = f.submit 'Set Goal', class: 'btn btn-primary btn-block'
